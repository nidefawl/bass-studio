
## DAW
- Upgrade to python > 3.11 on windows. (linux works flawlessly with >=3.14)
- When routing directly from input stage to a sidechain (secondary stereo pair) input of a plugin (Pro C2 in this case), the sidechain input is muted, instead it populates only the first stereo pair with the input stage signal, sidechain input is completly ignored. A workaround is: Put a gain plugin at 0db offset in between input stage and the sidechain input (secondary stereo pair) of Pro-C2
- After loading a project the history is populated with parameter change steps
- Adjusting dry/wet on the flutter parameter on AirWindows Clap Consolidated causes so much CPU lag that my PSU spikes and makes my sleeping speakers turn on (they turn on at -50db noise). Qt is the biggest pile of garbage
- Longer audio recordings ARE NOT SAVED!!!
- Plugins with 8 or more than audio outputs do not show their levels on the output meters (meter is less than 1 px width)
- Adjusting grooves on multip-clip-selections does not work when audio clips are in the selected list
- Fix shading on notes in noteeditor: Its impossible to differintiate between muted and non muted notes because of the very bright note shading (colors of notes have to much brightness compared to their clip color)
- Some Plugins like "Galactic64" have clearly audible cracke when automating its dry/wet parameter. This is because of missing block interpolation inside the plugin. This still occurs on my personal Port of the plugin. Add plugin interpolation for these "Black Sheeps". This also affects paramters like bigness (which seems more obvious than a dry/wet parameter jump without interpolation at block boundaries)
- Instead of multiple master tracks add premaster (on the fly rerouting)
- Implement Note Paint Mode
- Compile on raspberry
- Make hovering textfields like the one on "edit track name" accept and apply the input even when the user doesnt hit enter
- Theme Editor: 
  - Remove the save button 
  - Add context menu with options: "reset color" and "reset all"
  - Highlight mouse over rows in the properties and themes table
  - Toggle the mouse over theme color highlighting override in all columns
- Add a filter/smoothness parmater to automatable_param_t
- wave clips repitching does not work. probable cause is the task system refactor
- Fix macro 3rd party plugin preset parameter loading!!
- BUG: Notes gone after mousescroll. See share1/notes-gone-after-mousescroll-bug.mp4
- Fix macro preview deleting existing macro routings (during drag drop operation)
- Solo is not updating tracks correctly: bug-solo-not-working-correctly-when-using-child-track-input-stage-mixing.bsp
- Synth GPU: Latency compensation is incorrect. Compare a render between serum and synth-gpu on a submix bus
- wave clips do not show correct zoom when clicked (i.e. short sample like kick). When setting the zoom manually and reopening the clip its not restored
- The program requires some auto-update functionality: I think the best route is a installer that always replaces the current single installed version on the OS with the lastest one. Additionally I provide portable builds for windows and linux to allow running old versions
airwindows plugins to port:
- tube (soft sat)
- creature (soft sat)
- merge all todo lists
- cleanup mainctrl.cpp. we need dawinstance and mainctrl seperate. and project_controller_t (rename to ProjectController)
- try building a flatpak or appimage for linux
- get rid of cerial for serialization. use nlohmann json 
- when the preferences window is open, but in the background (behind the main window) it has to be brought to the front manually when reopening the preferences via menu